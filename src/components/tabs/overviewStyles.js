/**
 * @constant boxStyle
 * @description The style object for a box component.
 */
export const boxStyle = {
  p: 3,
  bg: "surfaceContainerLow",
  borderRadius: 4,
  width: "100%",
  maxWidth: ["100%", "700px"],
}

/**
 * @constant gridBoxStyle
 * @description The style object for a grid box component.
 */
export const gridBoxStyle = {
  gridTemplateColumns: ["1fr", "1fr 1fr"],
  gap: 3,
}

/**
 * @constant featureBoxStyle
 * @description The style object for a feature box component.
 */
export const featureBoxStyle = {
  display: "flex",
  textAlign: "left",
  padding: 2,
  borderRadius: 2,
  border: "1px solid",
  borderColor: "outlineVariant",
  alignItems: "center",
}

/**
 * @constant iconBoxStyle
 * @description The style object for an icon box component.
 */
export const iconBoxStyle = {
  pr: 3,
  color: "primary",
  fontSize: "32px",
}

/**
 * @constant headingStyle
 * @description The style object for a heading component.
 */
export const headingStyle = {
  marginBottom: 1,
}
